<%= form_with model: [:admin, @test, @question], local: true do |form| %>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title, id: :question_title %>
  </p><br>

  <p><%= form.submit %></p>
<% end %>